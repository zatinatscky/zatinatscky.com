"""
Слой реальных данных IVAN Terminal.

Состав пакета:
- registry.py  — метаданные всех индексов (единственный источник правды);
- store.py     — схема БД (index_meta / index_observation) и upsert;
- series.py    — сборка дневных рядов с forward-fill для отдачи фронту;
- sources/     — по модулю на внешний источник, каждый возвращает [(date, value)];
- sync.py      — оркестратор загрузки: индекс за индексом, ошибки изолированы.
"""
